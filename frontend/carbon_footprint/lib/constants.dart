// ignore_for_file: prefer_const_constructors, prefer_const_constructors_in_immutables

import 'package:flutter/material.dart';

// Temp Color Scheme
var defaultBackground = Colors.white;
var layerOneColor = Colors.grey[400];
var layerTwoColor = Colors.grey[500];

// General UI Rules

var boxDecorations = BoxDecoration(
  color: layerTwoColor,
  borderRadius: BorderRadius.circular(5),
);

// Scaffold Widgets
var rootAppbar = AppBar(backgroundColor: Colors.grey[900]);
